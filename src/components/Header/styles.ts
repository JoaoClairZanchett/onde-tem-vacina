import styled from 'styled-components';

export const Container = styled.div`
  background: var(--white);
`;

export const Wrapper = styled.div`
  height:70px;
  max-width: 100%;
  margin: 0 auto;
  padding-left: 25px;

  display: flex;
  align-items: center;
  background: linear-gradient(270deg, #679FAD -1.59%, #286E8C 91.32%);;
`;
